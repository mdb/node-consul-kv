{
  "name": "consul-kv",
  "version": "0.1.6",
  "description": "A micro library to get, set, and delete Consul K/V",
  "main": "consul.js",
  "scripts": {
    "test": "mocha && eslint *.js test/*.js",
    "tag": "git tag $npm_package_version && git push origin $npm_package_version",
    "publish": "npm run tag && npm publish"
  },
  "author": "Mike Ball",
  "license": "MIT",
  "dependencies": {
    "request": "2.88.0"
  },
  "devDependencies": {
    "eslint": "6.4.0",
    "mocha": "6.2.0",
    "nock": "11.3.4"
  }
}
